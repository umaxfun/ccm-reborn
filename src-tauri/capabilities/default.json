{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Least-privilege IPC for CCM Reborn's only application window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
